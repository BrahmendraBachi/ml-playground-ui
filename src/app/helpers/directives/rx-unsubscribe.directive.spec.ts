import { RxUnsubscribeDirective } from './rx-unsubscribe.directive';

describe('RxUnsubscribeDirective', () => {
  it('should create an instance', () => {
    const directive = new RxUnsubscribeDirective();
    expect(directive).toBeTruthy();
  });
});
